import { _decorator, Component } from 'cc';
const { ccclass } = _decorator;

/**
 * 平台组件
 * 用于标识平台节点
 */
@ccclass('Platform')
export class Platform extends Component {
    start() {
        // 平台初始化逻辑
    }
}
